// Clean pixelUtils.ts - All tracking code removed
// Ready for clean rebuild

export const initializePixel = () => {
  // Empty initialization - tracking code removed
};

export const firePixelEvent = () => {
  // Empty function - tracking code removed
};

// Add any other empty exports as needed for compatibility
